'use client';

export default function TermDropdown() {
//   const [terms, setTerms] = useState([]);

//   useEffect(() => {
//     // fetch terms from API when selectedAlphabet changes
//   }, [selectedAlphabet]);

//   return terms.map(term => <TermCard term={term} />);
 return (
    <div className="relative flex flex-2 flex-shrink-0">

    </div>
  );
}
